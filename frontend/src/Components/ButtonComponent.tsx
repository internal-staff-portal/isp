import React from "react";

//the props for the button component
type ButtonComponentProps = {
  //add props here
};

//the button component
export default function ButtonComponent({}: ButtonComponentProps) {
  return (
    <div>ButtonComponent</div>
  );
}
